#!/usr/bin/env python3
""" type annotated function that takes a float and returns it as a string """


def to_str(n: float) -> str:
    """ converts a float (n) and returns it as a string """
    return str(n)
